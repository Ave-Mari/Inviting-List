import styled from 'styled-components';

export const Wrapper = styled.section`

        ul {
            display: flex;

            li {
                display: flex;
                flex-direction: column;
                align-items: center;
            }

            img {
                width: 90px;
                height: 90px;
                border-radius: 50%;
                object-fit: cover;
            }
        }
    

`